<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.astero.myapplication01">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalcActivity" />
        <activity android:name=".DrawingActivity" />
        <activity android:name=".ChangeColorActivity" />
        <activity android:name=".GiphyActivity" />
        <activity android:name=".projectmanager.Activity.MainProjectManagerActivity" />
        <activity android:name=".projectmanager.Activity.TaskListActivity" />
        <activity android:name=".projectmanager.Activity.ProjectListActivity"></activity>
    </application>

</manifest>